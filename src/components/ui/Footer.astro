---
import Footer from '../layout/Footer.astro';
import BrandMark from '../brand/BrandMark.astro';

interface Props {
  lang: string;
}

const { lang } = Astro.props;
const currentLang = (lang === 'en' ? 'en' : 'ar') as 'en' | 'ar';
---

<Footer lang={currentLang} />

